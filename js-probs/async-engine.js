// prvoide implementation of constructor and process method.
/*
if no async method being processed by the queue, process the callback immediately
if one then also process immedieately
if two then next callback should be put into the queue 
 */
class asyncEngine {

}