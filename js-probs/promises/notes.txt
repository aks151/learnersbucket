1. promise.then vs Promise.resolve(promise).then
2. An object is iterable if it has a property whose key is Symbol.iterator, and the value of that property is a function.
3. Promise.finally() polyfill turned out to be a nightmare to understand